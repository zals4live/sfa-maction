export default defineAppConfig({
  ui: {
    colors: {
      // Primary aligned to KF Maction design token #1C4173 (see main.css --color-primary-*)
      primary: 'primary',
      neutral: 'slate'
    }
  }
})
